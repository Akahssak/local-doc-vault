{
  "name": "vault-local-docs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Admin-only PWA that stores uploaded PDFs in device-private local storage (OPFS), converts them to JSON, and provides regex search/filtering. Deployable to Firebase Hosting.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "lint": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:npx": "npm run build && npx -y firebase-tools deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.15.0",
    "pdfjs-dist": "4.4.168",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
